# test questionnaire
## Require:
* `node:16`
* `docker`
* free `:80` port
## Install
`make build` - once, build project (install .node_modules, vendor, copy .env files ...)
## Other
* `make up` - up server
* `make down` - down server
